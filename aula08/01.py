# definimos funcoes utilizando a palavra reservada def
def épar(x):
    return(x%2==0) # funcoes podem retornar um valor utilizando return
# algumas chamadas a funcao definida anteriormente
print(épar(2))
print(épar(3))
print(épar(10))
